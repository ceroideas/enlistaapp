import { CheckSubscriptionPipe } from './check-subscription.pipe';

describe('CheckSubscriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckSubscriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
